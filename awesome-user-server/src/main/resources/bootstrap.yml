spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-user-server
                profileSeparator: ','
                data-key: configuration
                format: YAML