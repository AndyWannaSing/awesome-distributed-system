spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-product-api
                profileSeparator: ','
                data-key: configuration
                format: YAML