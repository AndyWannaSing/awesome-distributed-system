spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-message-producer-server
                profileSeparator: ','
                data-key: configuration
                format: YAML