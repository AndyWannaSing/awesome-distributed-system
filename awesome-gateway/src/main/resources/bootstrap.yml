spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-gateway
                profileSeparator: ','
                data-key: configuration
                format: YAML