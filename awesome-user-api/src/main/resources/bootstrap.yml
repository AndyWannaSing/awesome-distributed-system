spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-user-api
                profileSeparator: ','
                data-key: configuration
                format: YAML