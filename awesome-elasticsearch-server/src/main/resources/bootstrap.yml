spring:
    cloud:
        consul:
            config:
                enabled: true
                prefix: config
                defaultContext: awesome-elasticsearch-server
                profileSeparator: ','
                data-key: configuration
                format: YAML